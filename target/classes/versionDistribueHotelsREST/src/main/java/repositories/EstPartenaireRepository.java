package repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import models.EstPartenaire;

public interface EstPartenaireRepository extends JpaRepository<EstPartenaire, Integer>{
}